<!DOCTYPE html>
<html lang="ja">

<head>
  <title>パスタイム</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <!-- CSS only -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="body-container">
    <header class="site-header">
      <div class="site-header-inner">
        <div class="contents-tidy header-width pt-2 pb-1">
        <div>
          <%=link_to root_path do %>
            <%= image_tag '/assets/store-logo-mark.png', style: 'width: 165px; height: 50px;', :alt => 'store-logo-mark' %>
          <% end %>
        </div>
        <div class="hamburger-menu">
          <input type="checkbox" id="menu-btn-check">
          <label for="menu-btn-check" class="menu-btn"><span></span></label>
          <div class="menu-content">
            <ul>
              <li>
                <% if current_user %>
                  <%= link_to "#{current_user.name}さん", user_path(id: current_user) %>
                <% else %>
                  <%= link_to '会員登録する方こちら！', new_user_registration_path %>
                <% end %>
              </li>
              <li>
                <% if current_user %>
                  <%= link_to 'WEB予約', reservations_path %>
                <% else %>
                  <%= link_to 'ログイン（すでに会員登録している方）', new_user_session_path %>
                <% end %>
              </li>
              <% if current_user %>
                <li>
                  <%= link_to '本日のクーポン', discount_path %>
                </li>
              <% end %>
              <% if current_page?(root_path) %>
                <li>
                  <button id="actionButton">INFORMATION</button>
                </li>
                <li>
                  <input type="button" value="MAP" onclick="scrollBlue()" />
                </li>
              <% end %>
              <% if current_user %>
                <li>
                  <%= link_to 'ログアウト', destroy_user_session_path, method: :delete%>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
      </div>
      <%= render 'layouts/flash' %>
      <%= render "devise/shared/error_messages", resource: resource %>
    </header>
    <main>
      <%= yield %>
    </main>
    <footer class="site-footer">
      <div class="site-footer-main">
        <a id="#page-top" class="page-top">
        </a>
        <p class="mt-3 mb-0">お電話で直接予約される方はこちら</p>
        <div class="telephone-number">
          <a href="tel:0120-17-1821" class="line-height">0120-17-1821</a>
        </div>
      </div>
    </footer>
  </div>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</body>

</html>
